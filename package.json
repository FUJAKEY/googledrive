{
  "name": "clouddrive",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["server", "client"],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client && npm run build --prefix server && rm -rf server/public && mkdir -p server/public && cp -r client/dist/* server/public/",
    "start": "npm run start --prefix server",
    "prisma:migrate": "npm run prisma:migrate --prefix server",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "test": "npm run test --prefix server",
    "prepare": "node ./scripts/prepare-husky.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7"
  }
}
