<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Aurora Drive | <%= title %></title>
  <link rel="stylesheet" href="/css/styles.css">
  <script defer src="/js/main.js"></script>
</head>
<body class="<%= bodyClass || '' %>">
  <div class="app-shell">
    <header class="app-header">
      <div class="container header-grid">
        <a class="brand" href="/">
          <span class="brand-logo">A</span>
          <span class="brand-text">
            Aurora<span>Drive</span>
          </span>
        </a>
        <button class="nav-toggle" id="navToggle" aria-label="Открыть меню">
          <span></span>
          <span></span>
          <span></span>
        </button>
        <nav class="main-nav" id="mainNav">
          <ul>
            <% if (currentUser) { %>
              <li><a href="/dashboard">Рабочая зона</a></li>
              <li><a href="/files/upload">Загрузить</a></li>
              <li><a href="/security">Безопасность</a></li>
              <li><a href="/support">Поддержка</a></li>
              <% if (currentUser.role === 'admin') { %>
                <li class="desktop-only"><a href="/admin/users">Администрирование</a></li>
              <% } %>
            <% } else { %>
              <li><a href="/security">Безопасность</a></li>
              <li><a href="/support">Поддержка</a></li>
            <% } %>
          </ul>
        </nav>
        <div class="user-menu">
          <% if (currentUser) { %>
            <div class="user-chip">
              <span class="user-name"><%= currentUser.name %></span>
              <span class="user-role"><%= currentUser.role %></span>
            </div>
            <div class="user-actions">
              <a class="link" href="/profile">Профиль</a>
              <a class="link" href="/profile/security">Безопасность</a>
              <% if (currentUser.role === 'admin') { %>
                <a class="link" href="/admin/users">Админ</a>
              <% } %>
              <form action="/logout" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button type="submit" class="btn btn-text">Выход</button>
              </form>
            </div>
          <% } else { %>
            <div class="auth-links">
              <a class="btn" href="/login">Войти</a>
              <a class="btn btn-secondary" href="/register">Регистрация</a>
            </div>
          <% } %>
        </div>
      </div>
    </header>
    <main class="page-content">
      <div class="container">
        <%- include('partials/flash'); %>
